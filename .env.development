# 开发环境默认值（对应 compose.yml 中的 Docker Compose 数据库）
# 此文件可提交入库，仅用于本地开发，生产环境请使用 .env.local 覆盖

# PostgreSQL 连接字符串（匹配 compose.yml 默认配置）
DATABASE_URL=postgresql://user:password@localhost:5432/elyhub

# Better Auth 会话签名密钥（仅供开发使用，生产环境必须替换）
BETTER_AUTH_SECRET=9pGgIPpbenlex27__dDmMqX4-rfuMN-6DV4dDlVWO9c

# Better Auth 回调地址
BETTER_AUTH_URL=http://localhost:3000
